server.port=${PORT:8004}
spring.application.name=shipping
management.endpoints.web.exposure.include=health,prometheus,info
management.endpoint.prometheus.enabled=true
management.metrics.tags.application=shipping

# Graceful shutdown — drain in-flight requests before exit
server.shutdown=graceful
spring.lifecycle.timeout-per-shutdown-phase=25s

# Structured JSON access + app logs to stdout (Spring Boot 3.4+)
logging.structured.format.console=ecs
server.tomcat.accesslog.enabled=true
server.tomcat.accesslog.directory=/dev
server.tomcat.accesslog.prefix=stdout
server.tomcat.accesslog.suffix=
server.tomcat.accesslog.buffered=false
server.tomcat.accesslog.pattern=%h %l %u %t "%r" %s %b %D "%{X-Request-Id}i"
# DB_SSL_MODE=REQUIRED for Azure Database for MySQL; DISABLED for a local mysql.
# The whole URL can also be overridden with DB_URL.
spring.datasource.url=${DB_URL:jdbc:mysql://${DB_HOST:mysql}:3306/cities?sslMode=${DB_SSL_MODE:DISABLED}&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USER:shipping}
spring.datasource.password=${DB_PASS:RoboShop@1}
spring.jpa.hibernate.ddl-auto=validate
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql
spring.sql.init.data-locations=classpath:data.sql
spring.jpa.defer-datasource-initialization=true
